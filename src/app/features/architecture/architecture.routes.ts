import { Routes } from '@angular/router';

import { Architecture } from './architecture';

export const ARCHITECTURE_ROUTES: Routes = [
  { path: '', component: Architecture, title: 'Architecture · TradingApp Ops' },
];
